{
  "name": "skill-or-be-skilled",
  "version": "0.1.0",
  "private": true,
  "description": "Portable skills for deliberate collaboration with AI agents",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/balmasi/skill-or-be-skilled"
  },
  "scripts": {
    "link": "node scripts/link-dev.mjs",
    "test": "python3 -m unittest discover -s tests -v",
    "validate": "npm run validate:skill && npm run validate:publish",
    "validate:skill": "node scripts/validate-skill.mjs",
    "validate:publish": "node scripts/validate-publish.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
